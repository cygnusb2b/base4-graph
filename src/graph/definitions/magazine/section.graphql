type MagazineSection {
  # fields directly on magazine.model::Section
  id: Int!
  name: String
  description: String
  publication: MagazineProductPublication
  issue: MagazineIssue
  fullName: String # @todo calculate in resolvers

  #fields from trait.platform::StatusEnabled
  status: Int

  #fields from trait.platform::Sequenceable
  sequence: Int
}
